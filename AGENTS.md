# Kentei iOS 開発ガイド

## 1. このプロジェクトの目的

本アプリは、インドネシア語検定の学習と、インドネシアで実際に行動できる力の獲得を一つの体験にする。単なる問題集ではなく、音声を聞いて判断し、生活シナリオを攻略し、できる行動が増えていく学習アプリとして実装する。

実装判断では、次の順に参照する。

1. 現在のユーザー指示
2. `docs/product-specification.md`
3. `docs/ios-architecture.md`
4. `docs/design-system.md`
5. `docs/content-and-data-specification.md`
6. `docs/delivery-plan.md`

仕様が競合するときは上記の優先順位と明示的な決定で解決する。解決できず、利用者の体験・データ・費用・外部契約を実質的に変える場合だけ確認する。影響しない作業は継続する。未確定事項を正式仕様として扱わない。
各文書は規範仕様を持ち、現在の実装状況・未実装項目は `docs/delivery-plan.md` を参照する。実装済みとの記載は動作確認済みを意味しない。

## 2. 変更範囲の原則

- 要求された機能を完成させる最小の変更にする。
- 無関係なリファクタリング、依存関係追加、プロジェクト設定変更を同時に行わない。
- 既存のユーザー変更を保持する。
- 機能変更には、同じ振る舞いを守る焦点を絞った回帰テストを追加する。
- 仕様変更時は、実装だけでなく該当する `docs/` も同じ変更で更新する。
- 外部サービス、課金、認証方式、Bundle ID、Signing Teamを推測で決めない。
- `com.example.*` はシミュレーター用デザインプロトタイプに限る。配布、アーカイブ、外部連携では正式Bundle IDへ置換されるまで使用しない。

## 3. プロダクト上の非交渉事項

- E級〜C級の通常問題は音声起点とし、回答前に問題文テキストを表示しない。
- E級〜C級は登録済みの問題、正解、解説で採点し、生成AIで回答や正解を作らない。
- B級〜A級だけが自由発話、要約、作文等のAI評価対象である。
- AI採点不能を不正解や0点として扱わず、再評価可能な状態にする。
- E〜C級の通常固定問題セッションは「10問 → 中間結果 → 10問 → 総合結果」とする。実戦チェックとB〜A級の自由回答課題は別の進行単位・状態機械で扱う。
- 各回答は回答直後にローカル保存し、途中離脱後に最後の完了問題から復帰できるようにする。
- 成果は正答率だけでなく、「空港で荷物を預けられる」などの行動達成として表示する。
- ゲーム演出で学習を不必要に遅らせない。演出は原則5秒以内でスキップ可能にする。
- 課金のために復習や進行を意図的に遅くしない。

## 4. iOS技術方針

- SwiftUIによるネイティブiOSアプリとする。
- 最低対応はiOS 17とし、Swift 6の厳格な並行性を前提にする。
- 単一アプリターゲットと必要な単体・UIテストターゲットを基本とし、必要になる前にパッケージ分割しない。
- 機能単位のフォルダと、Domain / Data / DesignSystem / Coreの責務境界を守る。
- 相互排他的なドメイン状態は `enum` 等で不正な組合せを防ぐ。独立したUIフラグにはBooleanを使ってよい。型付きIDは識別子の取り違えを防ぐ必要がある境界に用いる。
- UIから観測される状態変更は `@MainActor` 上で行う。
- 長時間タスクは構造化並行性を使い、画面離脱・セッション終了・再生停止でキャンセルする。
- 強制アンラップ、`try!`、未検査の型キャストを本番経路で使用しない。
- Apple標準機能で十分な場合は、第三者依存を追加しない。
- API、時計、UUID、音声再生、永続化などの外部・非決定的依存は、既存のプロトコルやクロージャ等で差し替え可能にする。単純な値のためだけに新しい抽象化を追加しない。

## 5. 状態とデータの所有権

- 学習セッションは明示的な状態機械で表現し、画面ごとに進捗を重複保持しない。
- 教材、問題、音声、タグはバージョン付きコンテンツとして扱う。
- 学習履歴、回答イベント、習得状態、復習予定は別の概念として保存する。
- 回答送信は再送可能な冪等操作にし、オフラインからの同期で重複回答を作らない。
- 永続化された保存形式、キー、列挙値は互換性契約である。互換性に影響する変更には必要な移行と旧データ読込テストを追加する。
- 派生値を画面に保存せず、正規の進捗データから算出する。

## 6. 音声方針

- 学習画面の中心操作は音声再生である。視覚装飾より再生開始の速さと確実さを優先する。
- 次問音声を先読みし、セッション開始時点で最低10問分をオフライン利用可能にする。
- E級のみ0.8倍再生を許容し、標準速度を基準記録とする。
- 再生回数、速度、完了・中断を学習イベントとして記録する。
- 電話、Siri、Bluetooth、アプリのバックグラウンド遷移による中断と復帰を処理する。
- 音声アセットはチェックサムとバージョンを検証し、破損時は再取得可能なエラーを表示する。

## 7. UI・キャラクター方針

- `docs/design-system.md` の色、余白、状態、文体を正とする。
- キャラクターは白〜薄灰の子猫、緑系の大きな瞳、ピンクの鼻と肉球、ターコイズのパーカーを基本形とする。
- キャラクターは「聞く・寄り添う・やさしく背中を押す」伴走者であり、学習者を責めたり不安にさせたりしない。
- キャラクターを正解の唯一の手掛かりにせず、問題・選択肢・音声操作を隠さない。
- ピンクは装飾と祝福に使い、白背景上の小さな本文色には使わない。
- 色だけで正誤や進捗を伝えない。アイコン、ラベル、形を併用する。
- Dynamic Type、VoiceOver、Reduce Motion、44pt以上の操作領域を守る。
- 初期UI言語は日本語とインドネシア語。ユーザー向け文字列を直接ハードコードしない。

## 8. キャラクター素材の扱い

- `docs/assets/character-reference.png` をキャラクター造形の正とする。アプリでは同じ毛色、顔、瞳、肉球、パーカーを維持した派生素材だけを使用する。
- 本番利用前に、権利、出典、商用利用可否、生成条件を確認する。
- 実装には表情・ポーズごとの透過素材、倍率別画像、意味を持つ場合の代替テキストが必要である。
- 参照資料にない新しい衣装、色、体型、人格を独断で正式設定にしない。

## 9. AI・ネットワーク・セキュリティ

- AI、音声認識、管理機能の秘密鍵をアプリに含めない。すべてサーバー側で保護する。
- 外部サービスの実接続前に選定・プロビジョニング・データ境界を決定する。未決定でも契約、モック、ローカル実装は進められる。
- プロバイダー秘密鍵はサーバーだけに置く。公開クライアント識別子と開発専用共有トークンを区別し、アプリに埋めた値を秘密として信頼しない。開発用共有トークンを本番配布の認証に使わない。
- 通信ではHTTPステータスを検証し、通信、認証、サーバー、デコード、キャンセルを区別して扱う。
- Authorization、録音、回答本文、個人情報をログへ出力しない。
- 録音の利用目的、保存期間、削除手段をUIと仕様の両方に明記する。
- AIへ送る情報は課題達成に必要な最小限とし、プロンプトとルーブリックのバージョンを保存する。

## 10. テストと完了条件

変更の受入条件とリスクに必要な項目だけを選ぶ。下記は全件必須の順序ではない。文書変更は整合性と差分の確認で完了する。

1. モデル・状態機械・採点・復習ロジックの単体テスト
2. 永続化の往復、移行、途中復帰テスト
3. APIの成功・失敗・キャンセル・不正レスポンステスト
4. 実アプリスキームのビルド
5. シミュレーターでの画面操作と音声再生確認
6. Dynamic Type、VoiceOver、ライト／ダーク、オフライン、音声中断の確認
7. 録音やオーディオセッション変更は必要に応じて実機確認

単体テストで対象アプリもコンパイルされる場合、同じ構成の別ビルドは不要。純粋なロジック・較正データ変更のために署名付き実機テストへ拡大しない。署名や端末機能の変更・依存がある場合に実機確認を選ぶ。必要な確認が通れば終了し、失敗や未解決の懸念がある場合だけ検証を広げる。

完了報告では、実施したテスト、直接確認した挙動、未実施項目と理由を分ける。ビルド成功だけをUI動作確認や実機確認として報告しない。
